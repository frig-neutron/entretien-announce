{
    "name": "entretien-announcer",
    "version": "0.0.1",
    "description": "Generates monthly Jira reports",
    "main": "dist/entretien-announce.js",
    "license": "GPL-3.0-only",
    "dependencies": {
        "dotenv": "^16.0.0",
        "jira.js": "2.11.0"
    },
    "devDependencies": {
        "jest": "^27.5.1",
        "typescript": "^4.6.2"
    },
    "scripts": {
        "test": "jest",
        "clean": "rm -rf dist",
        "copyfiles": "cp .env dist",
        "build": "yarn clean; tsc && yarn copyfiles && tsc --watch",
        "dev": "node dist/entretien-announce.js"
    }
}
